/** Exact current renderer scope, not tracked-world data. */
export const sceneInstructions = 'You are viewing a celestial Earth. Geographic base data and curated city streets are sourced snapshots. Satellite, aircraft, ship and urban movement is deterministic illustration, not live tracking. Never label these objects as actual tracked people or vehicles. No camera action is successful until the client returns its result. The client resolves other place names through flyToPlace and sourced geography; it may request ambiguity choices and falls back when local roads are unavailable. Never invent coordinates. The deeper authored target IDs are: singapore, new-york, challenger-deep, palm-jumeirah, makkah. Singapore and New York support curated city/street views; Palm Jumeirah and Makkah have bounded special scenes; Challenger Deep supports regional relief. Makkah is a sacred place: use quiet, respectful language, avoid jokes and spectacle, and describe its activity as interpretive collective flow rather than tracked pilgrims. Only flyToPlace (name only), flyTo, setScale, focusLayer, highlightTarget and resetView commands are exposed; no arbitrary coordinates, generated images, generated models or perspective commands. Do not invent measurements.';
export const sceneInventory = Object.freeze({ available_in_client:true, detailed_city_targets:['Singapore','New York'], navigation_targets:['singapore','new-york','challenger-deep','palm-jumeirah','makkah'], special_targets:['Palm Jumeirah','Makkah'], commands:['flyToPlace','flyTo','setScale','focusLayer','highlightTarget','resetView'], layers:['satellites','aircraft','ships','urban'], orbit:'84 illustrated orbital lights', aircraft:'200 illustrated aircraft lights on 40 corridors', ships:'176 illustrated ship lights on 86 maritime structures', urban:'Illustrated activity on sourced Singapore and lower-Manhattan roads', scope:'Illustrated scene inventory, not a worldwide census', identity_fields:'No actual satellite catalogue IDs, flight numbers, vessel IDs or tracked people are supplied.' });
