---
version: 1
slug: "app-constellation-loading-tsx"
primary_target: "app/constellation-loading.tsx"
related_targets: ["app/constellation-loading.module.css", "app/first-light-earth.ts", "docs/WIDER-OCEANS-V01017.md", "app/terra-experience.tsx", "lib/world/marine-corridors.ts", "lib/world/smooth-cables.ts", "lib/world/ocean-volume.ts", "lib/terra/network-composition.ts", "lib/terra/cable-filaments.ts", "lib/terra/engine.ts"]
---

# v0.10.17 — Wider ocean corridors and geographic first light, 18 September 2026

**Mode: Experience.** This narrow same-version extension preserves the established identity, Georgia/Arial pairing, pearl-and-gold Earth and generous darkness. Shared ocean approaches resolve into quieter companion strands. A small geographic Earth introduces the same material during loading. DESIGN.md remains the visual authority.

## Marine hierarchy

Thirty-one illustrative display spines extend the earlier seven across Pacific branches, Atlantic crossings, the Cape, East Africa and the southern Indian Ocean. Three bounded guides gather compatible crossings in the North Pacific, North Atlantic and South Atlantic. Water checks accept 234 of 240 candidate refinements; six retain source geometry. Source records, route identities, geographic endpoints and counts remain fixed.

Coincident accepted display paths share consistent strand separation, including paths traversed in reverse. Offsets taper to zero at endpoints and gather through bends. Already refined paths skip the older leg-gathering pass, removing the small Japan–Hawaii S-bend found during rendered review. Travelling lights follow the same prepared paths.

Selected long connections retain a bounded luminous core; finer companions remain subordinate. Teal shipping and lilac cables support Earth's geography. These are illustrated connections, not measured traffic, AIS tracks or surveyed alignments. Some source branches and tight coastal passages retain their schematic character.

The preceding current treatment remains: four soft, discontinuous strokes per existing basin across eight basins, preserving the 1,536-point budget. Current light/speed, pause and reduced-motion controls remain. Existing cyan satellites and smaller round pearl background stars retain their separate identities.

## Loading and handover — 19 September gathering prelude

The approved follow-up replaces the early Earth, name and tagline with 620 fine pearl/gold core grains and 42 sparse inward-curving arrivals. No geography is visible. The only copy is “Gathering the constellation…”, exposed as a status. A bounded Canvas 2D surface needs no downloaded image or WebGL engine. The warm core sits at the viewport centre, with scale derived from the same home framing and 42-degree camera as Genesis. Status uses 14px Arial with the existing cool secondary ink.

Readiness now means the main renderer has drawn its first frame. Genesis can hold at progress zero while the prelude dissolves over 800ms, then begins its unchanged 10.8-second sequence. There is no minimum loading hold. The text fades earlier in the handoff. Reduced-motion and saved motion-off keep the prelude still and bypass the dissolve; hidden tabs suspend its clock. Completion/unmount disposes its animation and resize observer.

Visual evidence: `.impeccable/review/gathering-prelude/` contains native Zen desktop, 390×844 responsive viewport, still-mode, consecutive handoff frames and actual application boot frames. The temporary review route holds the real prelude over the real engine; it is removed before build. Actual boot is separately checked. These are local rendered checks, not physical-phone, subjective listening or sustained performance acceptance. In-app browser automation was unavailable in this run; native browser inspection was used instead. Lifecycle checks cover first-render readiness, deferred core, idempotent start, skip, replay, reduced motion and disposal.

## Earlier wider-ocean evidence and limits

The fresh independent finish reviewer returned **ship**, validating all eight required captures with no material fixes for this narrow refinement. Captures in ../review/wide-oceans/: pacific.png, atlantic.png, indian-cape.png, mobile.png, user-1036.png, loading-desktop.png, loading-mobile.png and loading-user-1036.png. They support composition, panel fit and loading layout at 1280×720, 390×844 and 1036×998.

Loading captures hold the actual component on a temporary fixture removed before build. Actual boot handover was observed separately; the checked browser reported no console errors or horizontal overflow. The documenter inspected pacific.png and loading-mobile.png against the source and supplied read-only proposals for root to apply.

Existing primitive tokens and the sidecar remain unchanged. Static captures do not establish subjective motion quality, physical-device, other-browser, audio, quantitative geography or performance acceptance. Engineering checks and publication status remain separate from the visual disposition; see [implementation evidence](../../docs/WIDER-OCEANS-V01017.md).

## Earlier corridor evidence

The preceding 18 September corridor pass used seven illustrative spines across 34 existing paths and an abstract loader with 42 surrounding points, one central gold star and two faint threads. Its loader measured 250px, reducing to 210px on phones. The following evidence describes that historical pass.

The finish reviewer returned **ship** with no material fixes for this scope. Required captures in ../review/corridors/: desktop.png, atlantic.png, mobile.png, user-1036.png, coastal-close.png, loading-desktop.png, loading-mobile.png and loading-user-1036.png. They support composition, panel fit and loading layout at 1280×720, 390×844 and 1036×998. Loading captures hold the actual component on a temporary fixture removed before build. Actual boot handover was observed separately.

The documenter supplied read-only proposals; root applied this record. No new primitive tokens or sidecar changes were necessary. Static captures do not establish subjective motion quality, physical-device, other-browser, audio, quantitative geography or performance acceptance. Engineering checks and release state are separate from this visual disposition; see [implementation evidence](../../docs/CORRIDORS-FIRST-LIGHT-V01017.md).
